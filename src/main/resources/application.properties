# Nombre de tu aplicación
spring.application.name=pagoclinica

# Puerto del servidor (Railway usa la variable PORT)
server.port=${PORT:8080}

# --- Configuración de la Base de Datos para TU app pagoclinica ---
# Usando los valores específicos que te dio Railway:

spring.datasource.url=jdbc:mysql://metro.proxy.rlwy.net:18296/railway?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=FgqTAVHnvNAyqdSPPWDYzuncmcCWcOOE

# Driver de MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración del pool de conexiones Hikari (puedes mantener la del ejemplo anterior)
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Configuración de JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect # Correcto para MySQL reciente

# Otras configuraciones que tenías
spring.jackson.serialization.fail-on-empty-beans=false
logging.level.org.springframework.web=DEBUG

# Configuración para otros servicios (si aplica)
citas.service.url=${CITAS_SERVICE_URL:https://citaspoo-production.up.railway.app}